name: Swift

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2
      
    - name: Setup Xcode Version
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '13.3'
    # - name: Build Package
    #   run: swift build -v
    # - name: Run tests
    #   run: swift test -v
    - name: Build Example Wallet
      run: xcodebuild \
        -project Example/ExampleApp.xcodeproj \
        -scheme Wallet \
        -destination 'platform=iOS Simulator,name=iPhone 11,OS=13.0' \
        clean build
      # working-directory: ./Example
      # run: bundle exec fastlane test_wallet
    # - name: Test Dapp
    #   working-directory: ./Example
    #   run: bundle exec fastlane test_dapp